<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MoneyActivity"></activity>
        <activity android:name=".activites.PaymentActivity" />
        <activity android:name=".activites.CartActivity" />
        <activity android:name=".activites.CategoryProductActivity" />
        <activity android:name=".TestActivity" />
        <activity android:name=".activites.ProductDetailsActivity" />
        <activity android:name=".activites.SplashActivity" />
        <activity android:name=".activites.RegestrationActivity" />
        <activity android:name=".activites.OrderActivity" />
        <activity android:name=".activites.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activites.MainActivity" />
    </application>

</manifest>